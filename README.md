# Ereny Wagih – Portfolio (React + Vite + Tailwind + Framer Motion + R3F)

## Quick start

1. Extract the zip folder.
2. Open the project folder in VS Code.
3. Run:
   ```
   npm install
   npm run dev
   ```
4. Open the shown local URL (e.g. http://localhost:5173)

## Notes
- Replace sample videos in `src/App.jsx` projects array with your YouTube embed links (format: https://www.youtube.com/embed/<ID>).
- Replace email in contact form in `src/App.jsx`.
- You can deploy to Vercel or Netlify easily by pushing the folder to a GitHub repo.

If you want, I can push this project to a GitHub repo and set up automatic deploy to Vercel for you.
